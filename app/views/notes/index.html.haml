.wrapper_with_padding
	#notes.clearfix
		- unless @notes.blank?
			- @notes.each do |note|
				%a{ href: (url_for [note])}
					.note
						%p.title= note.title
						%p.date= time_ago_in_words(note.created_at)
		- else
			%h2 Agrega una nota
			%p Al parecer, no se ha creado ninguna nota aún ... Vamos a arreglar eso. ¿Por qué no sigues adelante y creas una nueva nota.
			%button= link_to "Nueva Nota", new_note_path